# CycleCare

A femtech cycle tracking mobile app using Clean Architecture.

## Setup

1. Run `flutter pub get`
2. Run `flutter pub run build_runner build`
3. Run `firebase flutterfire configure`
4. Run `flutter run`
